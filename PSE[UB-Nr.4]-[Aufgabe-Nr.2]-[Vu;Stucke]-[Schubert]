//*
*This program is used to add, multiply and divide two variables.
*
*With this program, you can add, multiply and divide any two variables. At the end, the output value is an integer.
*/
class Calculator{

 Integer getSum(Integer a, Integer b) {
  return a + b;
 }
 Integer getMul(Integer a, Integer b){
  return a * b;
 }
 Integer getDiv(Integer a, Integer b){
  return a / b;
 }
 Integer getArea(Integer length, Integer width){
  return length * widht;
 }
