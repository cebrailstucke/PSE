/**
*The class "Calculator" is able to add, multiply and divide two variables.
*
*With this program, you can add, multiply and divide any two variables. At the end, the output value is an integer.
*
*@author Cao Tu Vu 3389456
*@author Cebrail Stucke 3391486
*
*@ensures MIN_INT <= (value of the variables and the results) <= MAX_INT
*@requires variables are integers
*/
class Calculator{

/**
*The command getSum adds your selected variables and will return the result as an integer
*/
 Integer getSum(Integer a, Integer b) {
  return a + b;
 }
 /**
 *The command getMul multiplies your selected variables and will return the result as an integer
 */
 Integer getMul(Integer a, Integer b){
  return a * b;
 }
 /**
 *The command getDiv divides your selected variables and will return the result as an integer
 */
 Integer getDiv(Integer a, Integer b){
  return a / b;
 }
 /**
 *Calculate the area of a rectangle while multiplying the value of the length and width returns an Integer
 */
 Integer getArea(Integer length, Integer width){
  return length * width;
 }
